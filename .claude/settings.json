{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/data-md-reminder.sh"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f package.json ] && [ ! -d node_modules ]; then echo \"📦 node_modules 없음 — npm install 자동 실행 (1~2분 소요)\"; npm install && echo \"✅ 설치 완료\" || echo \"⚠️ 설치 실패 — 수동으로 npm install 필요\"; fi",
            "statusMessage": "📦 worktree 첫 진입 — npm install 확인 중",
            "timeout": 240
          }
        ]
      }
    ]
  }
}
