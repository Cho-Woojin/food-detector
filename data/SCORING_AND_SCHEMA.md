# 점수 체계 + 최종 데이터 명세서

식탐정(food-detector) 프로젝트의 식당 점수 산정 룰과 최종 데이터 파일 구조를 정리한 문서.

마지막 업데이트: 2026-05-09
관련 문서: [DATA_SOURCES.md](./DATA_SOURCES.md), [docs/05_DATA.md](../docs/05_DATA.md)

---

## 📑 목차

1. [점수 체계 개요](#-점수-체계-개요)
2. [데이터 점수 (0~50)](#1-데이터-점수-050)
3. [사장님 점수 (0~25)](#2-사장님-점수-025)
4. [사용자 점수 (0~25)](#3-사용자-점수-025)
5. [치즈 등급](#-치즈-등급)
6. [데이터 파이프라인](#-데이터-파이프라인)
7. [카테고리 매핑](#-카테고리-매핑)
8. [위험 태그 (식중독 위험)](#-위험-태그-식중독-위험)
9. [메뉴 힌트](#-메뉴-힌트)
10. [최종 파일 명세](#-최종-파일-명세)
11. [통계 요약](#-통계-요약)

---

## 🎯 점수 체계 개요

식당 한 곳의 **종합 점수 = 0~100점**, 다음 3개 컴포넌트의 합으로 구성된다.

| 컴포넌트 | 만점 | 출처 | 초기값 |
|---|---|---|---|
| **데이터 점수** | 50점 | 식약처 + 행안부 공공데이터 | 시그널 없을 시 25점 |
| **사장님 점수** | 25점 | 사장님 청소 인증 (최근 30일) | 인증 없을 시 0점 |
| **사용자 점수** | 25점 | 사용자 별점 리뷰 평균 | 리뷰 없을 시 0점 |

```
종합 점수 = 데이터(0~50) + 사장님(0~25) + 사용자(0~25)
         = 0 ~ 100점
```

### 설계 의도

- **데이터 50점**: 정적인 공공 시그널(위생등급·행정처분 등). 시간이 지나도 거의 변하지 않음.
- **사장님 25점**: 사장님이 적극적으로 청소 인증을 하는 식당에 가산. 자정 작용 유도.
- **사용자 25점**: 실제 방문 사용자의 평가. 데이터에 안 잡히는 위생 상태를 보강.

→ **데이터만으로는 실버(60+) 도달 불가**. 사장님·사용자 활동이 있어야 실버 이상으로 올라감. 골드(80+)는 데이터 + 사장님 + 사용자 모두 활성인 식당에만 부여.

---

## 1️⃣ 데이터 점수 (0~50)

식약처·행안부 공공데이터 시그널을 누적해 산정. **기본 25점 + 가감 시그널 → 0~50 클램프**.

### 가산 (위생/우수)

| 시그널 | 출처 | 점수 | 조건 |
|---|---|---|---|
| **위생등급 지정** (식약처 적합) | C004 (15060883) | **+20** | `hygiene_designated=true` (만료일 미경과) |
| 자율관리업소 평가 | I1540 | +10 | `eval_grade='자율관리업소'` AND 위생등급 지정 없음 |
| 일반관리업소 평가 | I1540 | +3 | `eval_grade='일반관리업소'` AND 위생등급 지정 없음 |
| 모범음식점 지정 | I1590 (15096282) | **+5** | `has_model=true` (위생등급과 독립적으로 가산) |

### 감산 (위반/위험)

| 시그널 | 출처 | 점수 | 비고 |
|---|---|---|---|
| 중점관리업소 평가 | I1540 | **-15** | 위생 미흡으로 중점 관리 대상 |
| 평가불능업소 | I1540 | -5 | 평가 거부/불가 |
| **영업소폐쇄** | I2630 | **-50** | 안전망용 (아래 §영업소폐쇄 처리 참조) |
| 영업정지 | I2630 | -20 | |
| 품목제조정지 | I2630 | -15 | |
| 과태료 / 과징금 | I2630 | -8 | |
| 시정명령 | I2630 | -3 | |
| 경고 | I2630 | -2 | |
| (그 외 처분) | I2630 | -3 | 미분류 처분의 기본값 |

### 계산식

```python
data_score = 25  # base

# 위생 / 평가 — 위에서부터 매칭되면 적용 (mutually exclusive)
if hygiene_designated:
    data_score += 20
elif eval_grade == '자율관리업소':
    data_score += 10
elif eval_grade == '일반관리업소':
    data_score += 3
elif eval_grade == '중점관리업소':
    data_score -= 15
elif eval_grade == '평가불능업소':
    data_score -= 5

# 모범음식점 가산 (위생/평가와 독립적)
if has_model:
    data_score += 5

# 행정처분 (cumulative)
PUNISH_DELTAS = {
    '영업소폐쇄': -50, '영업정지': -20, '품목제조정지': -15,
    '과태료': -8, '과징금': -8, '시정명령': -3, '경고': -2,
}
for punish_type in punish_types:
    data_score += PUNISH_DELTAS.get(punish_type, -3)

data_score = max(0, min(50, data_score))  # 0~50 클램프
```

### 점수표 산출 예시

| 케이스 | 계산 | 결과 |
|---|---|---|
| 위생등급 + 모범 | 25 + 20 + 5 | **50점 (만점)** |
| 자율관리 + 모범 | 25 + 10 + 5 | 40점 |
| 시그널 없음 (84%) | 25 | **25점** |
| 일반관리 | 25 + 3 | 28점 |
| 시정명령 1건 | 25 - 3 | 22점 |
| 중점관리 + 과태료 | 25 - 15 - 8 | 2점 |
| 영업정지 | 25 - 20 | 5점 |

### 영업소폐쇄 처리 (자기검토 노트, AI 분석 후 갱신)

- 식약처 I2630 행정처분 데이터를 AI(claude-opus-4-7)로 309건 분류한 결과:
  - **영업소폐쇄 241건의 100%, 영업허가·등록취소 24건의 100%가 "폐업 행정정리"** (시설 철거·폐업신고 미이행·사업자등록 말소). 위생 위험 시그널 아님.
  - 따라서 ROTTEN 트리거에서 punishTypes 기반 룰은 제거됐고, AI가 분류한 `flags.hygieneViolation` bool만 사용한다.
- 데이터 점수 가중치(영업소폐쇄 -50)는 그대로 유지:
  1. **데이터 시차 안전망** — 행정처분 적용일과 LOCALDATA 영업상태 갱신 사이 며칠~몇 주 갭. 그 윈도우에 잡혀도 -50으로 0점 클램프.
  2. **점수 산식 일관성** — 다른 처분과 동일한 형태로 정의해 미래 데이터 변경에도 코드 수정 불필요.
- 실질적으로 점수에 영향을 주는 처분은 **영업정지(-20), 과태료/과징금(-8), 품목제조정지(-15), 시정명령(-3)** 등 "현재 영업 중이지만 처분 받은" 케이스다.
- ROTTEN 분기는 처분 종류와 무관하게 **AI 분류 위생 위반(`hygieneViolation=true`) + 중점관리업소 + 사용자 과락**으로만 결정.

---

## 2️⃣ 사장님 점수 (0~25)

**최근 30일 이내 사장님 청소 인증 건수 × 2.5**, 0~25점으로 클램프.

| 인증 건수 (30일) | 점수 | 비고 |
|---|---|---|
| 0건 | 0점 | **기본값** (인증 안 한 경우) |
| 1건 | 2.5점 | |
| 2건 | 5점 | |
| 4건 | 10점 | |
| 6건 | 15점 | |
| 8건 | 20점 | |
| 10건 이상 | **25점 (만점)** | cap |

### 계산식

```ts
function ownerScore(verifications: Verification[], now = Date.now()): number {
  const THIRTY_DAYS = 30 * 24 * 60 * 60 * 1000;
  const recent = verifications.filter(v => now - v.timestamp <= THIRTY_DAYS);
  return Math.min(25, recent.length * 2.5);
}
```

### 구현 메모 (현재 단계)

- 사장님 청소 인증 기능은 **현재 개발 중** (별도 화면). 인증 데이터는 백엔드 부재 동안 클라이언트 storage(또는 향후 백엔드)에서 식당별로 누적.
- MVP 시점에서는 **모든 식당의 사장님 점수 = 0**이 정상. 점수 산식·UI 노출은 미리 셋업해 두고 인증 기능 완성과 함께 자동 반영.

---

## 3️⃣ 사용자 점수 (0~25)

**사용자 별점 리뷰 평균 × 5**.

| 별점 평균 | 점수 |
|---|---|
| 리뷰 0건 | **0점 (기본값)** |
| 1.0 | 5점 |
| 2.0 | 10점 |
| 3.0 | 15점 |
| 4.0 | 20점 |
| 5.0 | **25점 (만점)** |

### 계산식

```ts
function userScore(reviews: Review[]): number {
  if (reviews.length === 0) return 0;
  const avg = reviews.reduce((acc, r) => acc + r.rating, 0) / reviews.length;
  return avg * 5;  // 0~25
}
```

### 구현 메모

- 별점은 1~5 정수. 평균은 소수점 가능.
- 리뷰 작성 시 부가 입력(긍정/부정 태그·이물질·사진·한줄평)은 점수에 직접 반영하지 않음. **별점 평균만 점수 산식에 사용**.
- 이물질 신고는 UI에 강조 표시(빨간 배너 등)하지만 점수 차감 X. 운영진/사장님이 보고 대응할 시그널 역할.

---

## 🧀 치즈 등급

종합 점수(0~100)와 **과락 조건**을 결합해 4단계로 분류.

| 등급 | 조건 | 색상 |
|---|---|---|
| **🧀 골드 치즈** | score ≥ 80 | `#F1C40F` (golden) |
| **🧀 실버 치즈** | 50 ≤ score < 80 | `#94A3B8` (silver) |
| **🧀 브론즈 치즈** | score < 50 (기본) | `#CD7F32` (bronze) |
| **🚨 썩은 치즈** | score < 50 AND 과락 조건 | `#FF3B30` (danger red) |

### 썩은 치즈 과락 조건

**점수가 50 미만일 때만** 평가. 50점 이상이면 과락이 있어도 무조건 실버 이상.

> 참고: 데이터 점수 만점 = 50이라 "위생등급+모범" 식당은 자동으로 50점 → 실버. 사장님·사용자 활동이 없어도 데이터 시그널이 강하면 실버 진입 가능.

| 과락 | 조건 |
|---|---|
| **과락 1** (사용자) | 사용자 리뷰 ≥ 10개 **AND** 사용자 점수 ≤ 10/25 (별점 평균 ≤ 2.0) |
| **과락 2** (식약처 평가) | 중점관리업소 평가 (위생 미흡으로 식약처가 직접 분류) |
| **과락 3** (AI 분류 위생 위반) | `flags.hygieneViolation = true` — AI가 식약처 I2630 행정처분의 위반사유(VILTCN)를 분석해 "식품 위생 직결 위반"으로 분류한 경우. 자세한 카테고리·분류 결과는 `data/violations-classified.json` 참조. |

**과락 1 OR 과락 2 OR 과락 3** 셋 중 하나만 달성해도 썩은 치즈.

#### 행정처분 종류만으로 판단하지 않는 이유

옛 룰은 처분 종류(영업정지·영업소폐쇄·과태료·과징금)를 ROTTEN 트리거로 썼지만, AI(claude-opus-4-7)로 309건 분류한 결과 처분 종류와 위생 위험은 약하게 연관됨:

| 처분 | 건수 | AI가 위생 직결 분류한 건수 | 비율 |
|---|---|---|---|
| 영업소폐쇄 | 241 | 0 | 0% (시설철거·폐업 행정정리) |
| 영업허가·등록취소 | 24 | 0 | 0% (사업자등록 말소·행정정리) |
| 영업정지 | 44 | **3** | 6.8% (이물·유통기한·무등록 식품) |

→ 영업정지의 위반사유는 거의 다 청소년 주류·유흥접객·성매매 등 사회·도덕 위반(식중독 위험과 무관). 처분 종류로만 판단하면 식탐정의 본질(위생·식중독)과 어긋나므로, AI가 분류한 `hygieneViolation` bool을 직접 사용한다.

`punishTypes`/`punishReasons`는 통계·UI 표시용으로 보존되며 ROTTEN 트리거에는 사용 안 함.

### 등급 결정 로직

```ts
function deriveGrade(input: {
  score: number;            // 0~100 종합 점수
  flags: {
    evalGrade: string;          // '자율관리업소' | '일반관리업소' | '중점관리업소' | '평가불능업소' | ''
    punishTypes: string;        // pipe-separated (예: "영업정지|과태료"). UI/통계용, ROTTEN 트리거 X.
    hygieneViolation: boolean;  // AI(claude-opus-4-7) 분류 결과 — 위생 직결 위반 여부
  };
  userScore?: number;       // 0~25, default 0
  userReviewCount?: number; // default 0
}): 'GOLDEN' | 'SILVER' | 'BRONZE' | 'ROTTEN' {
  const { score, flags, userScore = 0, userReviewCount = 0 } = input;

  // SILVER 임계값(50) 이상이면 과락 무시 — 실버/골드만
  if (score >= 80) return 'GOLDEN';
  if (score >= 50) return 'SILVER';

  // score < 50 — 썩은 치즈 조건 평가
  const userFail = userReviewCount >= 10 && userScore <= 10;
  const evalFail = flags.evalGrade === '중점관리업소';
  const hygieneFail = !!flags.hygieneViolation;

  return (userFail || evalFail || hygieneFail) ? 'ROTTEN' : 'BRONZE';
}
```

### 등급 분포 의도

- **MVP 시점**: 사장님·사용자 점수 0이므로 대부분 BRONZE.
  - 위생등급 + 모범 (데이터 만점 50) → 50점 → **SILVER** (데이터만으로 진입 가능)
  - 위생등급 단독 (45) → 45점 → BRONZE
  - 시그널 없음 (25) → 25점 → BRONZE
- **사장님·사용자 활동 누적 후**: 더 많은 식당이 SILVER → GOLDEN(80+)으로 격상.
- **ROTTEN**: 처음부터 행정처분 있는 식당 일부가 즉시 ROTTEN으로 분류.

---

## 🔄 데이터 파이프라인

```
┌─────────────────────────────────────────────────────────────────────┐
│ 1단계 — 원본 수집 (외부 파이프라인)                                   │
│  • LOCALDATA bulk (일반/휴게)                                         │
│  • 식품안전나라 OpenAPI (C004, I1590, I1540, I2630)                   │
│  • 카카오 Local API (좌표 보강 + 카테고리 검색 신규 발굴)              │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 2단계 — 정제 + 시그널 결합 (외부 파이프라인)                          │
│  → 사업장명 정제, 좌표 통일 (EPSG 5174 → 4326)                       │
│  → 위생/처분/모범 데이터 매칭                                         │
│  → 결과: _archive/restaurants_with_scores.csv                        │
│                                                                      │
│  CSV 컬럼은 "원시 시그널 + 옛 점수" 형태. 옛 score 컬럼은 무시되며   │
│  split 단계에서 데이터 점수(0~50)를 새 룰로 재계산.                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 3단계 — 자치구별 분할 + 데이터 점수 계산                              │
│  scripts/split-restaurants.js                                        │
│   → 데이터 점수 계산 (0~50)                                          │
│   → 카테고리 재매핑 (raw → 통합)                                     │
│   → 위험 태그 부여 / 메뉴 힌트 추출                                  │
│   → 출력: data/by-gu/{slug}.json (25개)                              │
│   → 출력: data/restaurants-index.json                                │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 4단계 — 앱 (Expo + RN)                                               │
│  • static: by-gu/*.json 로드 (lazy import per 자치구)                │
│  • runtime: ownerScore (utils/owner.ts) + userScore (utils/reviews) │
│  • total = data + owner + user → deriveGrade(...)                    │
│  • 마커 색상·점수 표시·치즈 뱃지 모두 위 결과로 통일                  │
└─────────────────────────────────────────────────────────────────────┘
```

> **분리 원칙**: 정적 데이터 점수(0~50)는 JSON에 박혀 있고, 동적 점수(사장님·사용자)는 클라이언트가 매번 합쳐서 표현. 새 리뷰·인증이 들어와도 JSON 재빌드 없이 즉시 UI 반영.

---

## 📂 카테고리 매핑

LOCALDATA·카카오의 raw 카테고리 → 식탐정 통합 카테고리로 변환 (`scripts/split-restaurants.js`의 `RAW_MAP` + `NAME_RULES`).

### 통합 카테고리 17종

| 통합 카테고리 | 매핑되는 raw 카테고리 (대표) |
|---|---|
| **한식** | 한식, 냉면집, 구내식당, (이름에 국밥/한정식/백반 포함) |
| **중식** | 중식, 중국식 |
| **일식** | 일식, 횟집, 복어취급, (이름에 라멘/돈까스/스시 포함) |
| **양식** | 양식, 경양식, 패밀리레스트랑, (피자/파스타/스테이크/이탈리) |
| **분식** | 분식, (김밥/떡볶이/순대) |
| **도시락** | 김밥(도시락), 도시락, (한솥/본도시락) |
| **치킨** | 치킨·호프, 통닭(치킨), 호프/통닭, (BBQ/BHC/교촌 등) |
| **카페디저트** | 까페, 카페, 커피숍, 다방, 아이스크림, 떡카페, (스타벅스/투썸/이디야 등) |
| **고기** | 식육(숯불구이), (삼겹살/한우/곱창) |
| **뷔페** | 뷔페, 뷔페식 |
| **술집** | 정종/대포집/소주방, 감성주점, 단란주점, 유흥주점, (포차/호프/주류) |
| **패스트푸드** | 패스트푸드, (맥도날드/버거킹/롯데리아 등) |
| **아시안** | 외국음식전문점(인도, 태국 등), (쌀국수/팟타이/마라탕 등) |
| **찜탕** | 탕류(보신용), (감자탕/뼈해장국/아구찜 등) |
| **샌드위치** | (이름에 서브웨이/샌드위치/에그드랍) |
| **샐러드** | (이름에 샐러드/스윗밸런스 등) |
| **기타** | 출장조리, 이동조리, 푸드트럭, 백화점, 철도역구내, 고속도로 등 |

### 카테고리 결정 우선순위

1. **이름 키워드 매칭** (`NAME_RULES`) — 가장 구체적인 패턴 우선 (예: "스타벅스" → 카페디저트)
2. **raw 카테고리 매핑** (`RAW_MAP`) — LOCALDATA 업태구분명 또는 카카오 카테고리 변환
3. 매칭 실패 시 → **"기타"**

전체 매핑 룰은 [scripts/split-restaurants.js](../scripts/split-restaurants.js) 참조.

---

## ⚠️ 위험 태그 (식중독 위험)

이름·원본 카테고리에 키워드 매칭으로 부여 (`scripts/split-restaurants.js`의 `RISK_RULES`).

| 태그 | 의미 | 키워드 예시 |
|---|---|---|
| `raw_fish` | 생선회/사시미/스시 | 회집, 활어회, 모듬회, 스시, 사시미, 오마카세, 회덮밥, 물회, 연어회, 광어회 |
| `raw_meat` | 육회/생고기 | 육회, 육사시미, 생고기, 타다끼, 뭉티기 |
| `shellfish_raw` | 생굴/조개구이 | 생굴, 굴집, 석화, 홍합, 조개구이, 조개찜, 소라, 전복회, 꼬막 |
| `egg_raw` | 날달걀 | 날달걀, 계란회, 난황 |
| `fugu` | 복어 | 복어, 복지리, 복국, 복매운탕 |
| `raw_chicken` | 닭회 | 닭회 |

각 식당의 `riskTags` 배열에 해당 태그들이 들어감 (복수 가능).

> 활용 예: 면역력 약한 사용자(어린이·임산부·고령자)에게 `raw_fish` 태그 식당은 경고 표시. 또는 식중독 예측지수(EN03, 향후 통합 예정) 높은 시즌에 강조.

---

## 🍽 메뉴 힌트

식당 이름에서 대표 메뉴를 추정 (`scripts/split-restaurants.js`의 `MENU_HINTS`). 검색·필터링 보조용.

키워드 사전 (60+개):
- **한식**: 삼겹살, 갈비, 곰탕, 설렁탕, 추어탕, 감자탕, 뼈해장국, 김치찌개, 된장찌개, 닭갈비, 비빔밥, 냉면, 족발, 보쌈, 곱창, 막창...
- **분식**: 김밥, 떡볶이, 튀김, 순대, 라볶이...
- **일식**: 돈까스, 우동, 라멘, 스시, 초밥, 오마카세, 소바...
- **중식**: 짜장면, 짬뽕, 탕수육, 마라탕, 양꼬치, 딤섬...
- **아시안**: 쌀국수, 팟타이, 분짜, 반미, 포케, 카레...
- **양식**: 피자, 파스타, 스테이크, 햄버거, 타코...
- **카페디저트**: 케이크, 마카롱, 도넛, 와플, 빙수...

각 식당의 `menuHints` 배열에 매칭된 키워드들이 들어감.

---

## 📁 최종 파일 명세

### 7-1. `data/by-gu/restaurants-{slug}.json`

자치구별 분할 JSON. 앱에서 사용자 위치/선택에 따라 lazy load (각 ~3-15MB).

파일 명명: 영문 슬러그 (`gangnam`, `mapo`, `jongno` 등 25개)

각 식당 객체:

```json
{
  "id": "3130000-101-2024-01063",
  "name": "명동 칼국수",
  "gu": "마포구",
  "category": "분식",                // 통합 카테고리
  "categoryRaw": "분식",             // 원본 LOCALDATA/카카오 카테고리
  "addr": "서울특별시 마포구 도화동 555 ...",
  "roadAddr": "서울특별시 마포구 마포대로 33, ...",
  "phone": "",
  "lat": 37.54004,
  "lng": 126.945471,

  "score": 50,                       // 데이터 점수 (0~50). 앱이 owner/user를 더해서 종합 점수 계산
  "breakdown": {                     // 데이터 점수 구성
    "data": 50,                      //   = score (= 25 base + signals)
    "hygiene": 20,                   //   위생등급 가산
    "evalDelta": 0,                  //   평가 ± (자율 +10 / 일반 +3 / 중점 -15 / 평가불능 -5)
    "punish": 0,                     //   행정처분 누적
    "model": 5                       //   모범음식점 가산 (5 또는 0)
  },
  "flags": {
    "hygieneDesignated": true,
    "hasModel": true,
    "punishCount": 0,
    "punishTypes": "",               // pipe-separated, 예: "영업정지|과태료"
    "evalGrade": "",                 // 자율/일반/중점/평가불능/''
    "punishReasons": "",             // AI 분류한 위반사유 요약 pipe-separated (예: "이물혼입(바퀴벌레)")
    "hygieneViolation": false        // AI 분류 결과 위생 직결 위반 여부 (claude-opus-4-7)
  },

  "riskTags": [],                    // 위험 태그 (raw_fish 등)
  "menuHints": []                    // 메뉴 힌트 (삼겹살 등)
}
```

> **종합 점수·등급은 JSON에 저장하지 않음**. 앱이 매 렌더 시 `data + owner + user`를 계산하고 `deriveGrade()`로 분류.

### 7-2. `data/restaurants-index.json`

메타 정보 + 자치구별 식당 수 인덱스. 앱이 처음 시작 시 로드해서 어떤 자치구 파일을 다운받을지 결정.

```json
{
  "meta": {
    "totalCount": 152238,
    "gus": ["강남구", "강동구", ...25개],
    "gusCount": { "강남구": 15507, ... },
    "guSlug": { "강남구": "gangnam", ... },
    "categories": [...],
    "lastUpdated": "2026-05-09"
  },
  "index": []
}
```

---

## 📊 통계 요약

### 데이터 규모

| 항목 | 수치 |
|---|---|
| 정제 후 마스터 | 153,254건 |
| CSV 변환 후 (자치구 있는 것만) | 152,238건 |
| 좌표 보유율 | 99.8% |
| 자치구 분포 | 강남 15,507 / 마포 9,533 / 송파 9,310 / 영등포 8,397 / 서초 7,457 / ... |

### 점수 시그널 매칭 (데이터 점수에 반영)

| 시그널 | 매칭 식당 수 | 비율 |
|---|---|---|
| 위생등급 지정 (+20) | 24,578 | 16.0% |
| 모범음식점 (+5) | 10,499 | 6.8% |
| 위생관리평가 (±) | 3,011 | 2.0% |
| 행정처분 (-) | 2,496 | 1.6% |
| **하나라도 매칭** | 42,353 | 27.6% |

### 데이터 점수 분포 (예상, MVP 시점)

| 데이터 점수 | 식당 수 | 비율 | 비고 |
|---|---|---|---|
| 50 (만점) | ~14,000 | ~9% | 위생등급 + 모범 |
| 45 (위생등급) | ~14,000 | ~9% | 위생등급 단독 |
| 25 (시그널 없음) | ~110,000 | ~72% | 기본값 |
| 22~28 (소규모 가감) | ~10,000 | ~7% | 일반관리·시정명령 등 |
| 0~10 (강한 감산) | ~3,000 | ~2% | 중점관리·영업정지·과태료 등 |

→ MVP에서는 **거의 모든 식당이 BRONZE** (데이터 점수만으로는 60 미달). 사장님·사용자 활동이 쌓이면 SILVER/GOLDEN 비율 증가.

---

## 🔧 재실행 가이드

데이터 갱신 시 다음 순서로 실행:

```bash
# 1. (외부) 원본 재수집 + CSV 빌드
#    → _archive/restaurants_with_scores.csv 갱신

# 2. 자치구별 분할 + 데이터 점수 재계산
node scripts/split-restaurants.js

# 3. 결과 확인
#    data/by-gu/*.json 갱신 (25개 파일)
#    data/restaurants-index.json 갱신
```

CSV 없이 기존 by-gu JSON만 갖고 점수 룰을 바꿨을 때:

```bash
# 기존 JSON의 flags를 입력으로 새 score/breakdown만 재계산
node scripts/recompute-scores.js
```
